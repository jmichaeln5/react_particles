<% require 'json' %>
<% default_script = {
    name: "#{namespace}", private: "true",
      "dependencies": {},
      "scripts": {
        "build": "rollup -c rollup.config.js"
      }
  }
%>

<%= JSON.pretty_generate(default_script) %>
